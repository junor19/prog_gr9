import unittest 
import csv
from datetime import datetime

sys.path.insert(0, os.path.abspath(os.path.join(os.getcwd(), '../data')))

import raa_data_d
import raa_data_m

class testmonth(unittest.TestCase):
    def test_date(self):
        with open(raa_data_d.csv, 'r') as csvfile:
            reader = csv.reader(csvfile)
            for row in reader:
                date = datetime.strptime(date_string,"2020-01-01")
        year = raa_data_d
        forventet_dato = datetime(2020)
        self.assertIn(forventet_dato, raa_data_d)
        
if __name__ == "__main__":
    unittest.main()